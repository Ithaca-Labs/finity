{
  "name": "finity",
  "private": true,
  "version": "0.1.0",
  "description": "Ledger-governed commerce network on Hedera",
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "demo": "tsx scripts/demo.ts"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "tsx": "4.20.6",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
