# summarize-lite@1

Per-unit x402 summarization provider: 0.01 HBAR per 1,000-character unit.

This phase ships the deterministic provider boundary and quote validation. The
Connector Vault PAB injection path is intentionally deferred to the later
`vault-worker` module.
