{
  "name": "@finity/policy-engine",
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@finity/schemas": "workspace:*"
  },
  "devDependencies": {
    "@finity/mandate-compiler": "workspace:*"
  },
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.json && node scripts/emit-policy-hash.mjs", "typecheck": "tsc -p tsconfig.json --noEmit", "test": "vitest run --passWithNoTests", "lint": "tsc -p tsconfig.json --noEmit" }
}
